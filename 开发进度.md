# iPhone 共享助手开发进度

更新时间：2026-08-26 · 当前版本：3.34

## 产品定位

给本人和家人使用的 Windows 小工具，核心用途只有一件事：

> 选择电脑上的文件夹并开启 SMB 共享，让 iPhone“文件”App 可以访问它。

不做云同步、会员、家庭成员权限系统或电脑端文件管理器。

## 当前状态（3.34）

- 单文件源码：`源码/主程序-3.0.ps1`（后台逻辑 + WPF 界面全在一个文件）。
- 打包产物为完全独立的单文件 EXE；为保留“暂停后仍在列表”所需的管理记录，运行时会在 `C:\ProgramData\iPhone共享助手\managed-shares.json` 保存共享名称、文件夹路径、暂停状态、创建时间和权限信息，不保存密码，也不保存文件内容。
- 卡片式无边框界面：自定义标题栏与窗口按钮、自定义确认框/错误框，视觉与设计原型一致。
- 版本约定：每打包一次版本号递增（3.1 → 3.2 → …），旧版本 EXE 一律保留、不覆盖。
- 新增共享详情页：点击卡片「详情」进入，展示状态、路径、手机地址、来源与访问权限，支持返回列表。
- 共享编辑：详情页可修改共享名称（改名）与文件夹路径（换路径），本软件共享直接编辑，外部共享需强确认。

## 功能清单

- 扫描本机普通共享，隐藏 `C$`、`ADMIN$`、`IPC$` 等系统管理共享。
- 选择文件夹开启共享：自动用文件夹名作为共享名，同步 SMB 权限 + NTFS 权限 + SMB 服务 + 防火墙 445。
- 本软件共享支持“暂停共享”：只移除 SMB 映射，记录仍保留在列表，可点击“重新开启”；“删除记录”会移除 SMB 映射和管理记录，但绝不删除文件夹或文件。外部共享仍需明确确认后关闭；系统管理共享始终不可关闭。
- 共享详情页：查看共享名称、状态、路径、手机地址、来源（本软件/外部）与访问权限（只读展示）。
- 编辑共享：修改共享名称或文件夹路径；改名自动复制访问账号与归属标记，换路径同步 NTFS 权限；外部共享编辑前需强确认。
- 复制 iPhone 使用的 `smb://电脑IP/共享名` 地址。
- 底栏显示手机连接地址、登录账号（仅用户名）与密码提示。
- 共享归属标记使用 SMB 共享自身的 Description 字段（`iPhone共享助手`），并结合本地管理元数据恢复已暂停共享。
- 启动时管理员检查；`SmbShare` 模块显式导入并带语言包回退。

## 当前源码结构

- `源码/主程序-3.0.ps1`：单文件主程序（含 WPF 界面层），运行入口。
- `打包.bat` + `build.ps1`：打包入口与辅助脚本（检查 PS2EXE、输出文件名与版本号随版本递增）。
- `资源/222.ico`：程序图标；`资源/UI原型/iphone-share-ui.html`：界面视觉设计原型。
- `已知问题与踩坑记录.md`：历史问题归档（打包/运行环境/界面/权限/需求变更）。
- `核心源码.txt`：当前源码的 TXT 副本（与 `源码/主程序-3.0.ps1` 内容一致）。
- `历史存档/`：1.0、2.0-旧版、2.0-单文件版、2.6-单文件版、3.0 构建等旧版本备份，不再参与运行。
- `打包输出/`：3.0 起的各版本 EXE，全部保留。

## 运行方式（不打包）

在项目根目录以管理员身份运行：

```powershell
powershell.exe -ExecutionPolicy Bypass -File ".\源码\主程序-3.0.ps1"
```

## 手机连接

1. 电脑和 iPhone 连接同一个 Wi-Fi。
2. 在软件中选择文件夹并开启共享。
3. 点击共享卡片上的“复制地址”，得到 `smb://电脑IP/共享名`。
4. iPhone 打开“文件”App → 右上角 `...` →“连接服务器”。
5. 输入 SMB 地址，选择“注册用户”，使用 Windows 用户名和密码登录。

### iPhone 提示“内容不可用，没有权限”

说明 iPhone 已找到电脑和共享，但账号权限没通过：

1. 确认软件是以管理员身份运行的。
2. 使用开共享那台电脑的 Windows 账号登录（电脑名\用户名），密码是 Windows 账户密码，不是 PIN。
3. 若 iPhone 有旧缓存，删除该服务器连接后重新连接。

软件只把当前 Windows 账号写入 SMB/NTFS 权限，不保存任何密码。

## 当前已知限制

- Windows Hello PIN 不等于 SMB 登录密码。
- 尚未在不同 Windows 版本和账户类型上完成完整实机验收。
- 启动时的“需要管理员权限”提示仍为系统 MessageBox（其余对话框已自定义样式）。
- 界面以过程式代码构建为主；可选的 DataTemplate 大重构有视觉回归风险，暂缓。
- 详情页仅做访问权限只读展示，暂不提供多账号权限编辑。

## 下一步建议

1. 在真实家庭电脑上验收“开启共享 → iPhone 连接并读写”。
2. 验证共享详情页的编辑（改名、换路径）与外部共享强确认流程。
3. 验证外部共享关闭的确认流程，系统管理共享不可关闭。
4. 按真实使用反馈微调界面与流程。
5. 全部稳定后重新打包分发（版本号继续递增）。

## 安全约定

- 不自动删除文件夹、文件或 Windows 用户。
- 暂停或删除共享前必须确认，并明确说明不会删除实际文件；删除记录只删除 SMB 映射和本软件记录。
- 系统管理共享不提供关闭操作。
- 不保存 Windows 用户密码。
- 外部共享关闭前必须有明确确认。
